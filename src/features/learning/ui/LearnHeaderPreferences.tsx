"use client";

import { PreferenceIconGroup } from "@/shared/ui/PreferenceIconGroup/PreferenceIconGroup";

export function LearnHeaderPreferences() {
  return (
    <PreferenceIconGroup hint="Adjust how the learn app looks on this device." />
  );
}
